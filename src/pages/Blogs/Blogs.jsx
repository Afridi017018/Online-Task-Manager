import React from 'react';
import BlogCards from '../../components/BlogCards/BlogCards';

const Blogs = () => {
    return (
        <div>
           <BlogCards />
        </div>
    );
};

export default Blogs;